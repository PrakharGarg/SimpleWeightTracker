<div class="container">
    <% flash.each do |key, value| %>
      <% if value != "You need to sign in or sign up before continuing." && value != "Signed in successfully." %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>
    <% end %>
  </div>
